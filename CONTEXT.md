# GoWithFlow 项目上下文

> 本文件记录项目关键决策、当前状态与待办，便于新 AI 快速接手，减少上下文重复。每次完成重要工作后，应更新本文件、提交 git、并部署到远程服务器。

## 开发偏好

详见项目根目录 `PREFERENCE.md`：环境隔离、显式禁用代理、时区显式、数据源先验证、重大改动先本地验证再上线等。

## 项目简介

- **名称**：GoWithFlow
- **目标**：通过自然语言理解用户的“模糊出行意图”，推荐符合组合氛围的周末地点。
- **产品形态**：MVP 先以 Web 应用验证，后续接入微信小程序。

## 用户偏好

记录在 PREFERENCE.md 中

## 关键决策

| 维度 | 决策 |
|---|---|
| 城市 | 仅北京 |
| 交通 | 仅自驾（默认开车），公共交通/骑行写入后续规划 |
| 输入 | 自然语言短句，允许不完整；缺失关键信息时只追问一次 |
| 输出 | 3 个推荐，同时展示列表与地图 |
| 数据 | 本地 JSON 种子 POI 库，MVP 不爬取大众点评/小红书 |
| 反馈 | 底部固定按钮“哪里不对？”，替换全部 3 个推荐 |
| 后端 | Python 3.11 + FastAPI，不使用向量数据库 |
| AI | DeepSeek API（`deepseek-v4-flash`） |
| 天气 | Open-Meteo（无需 API Key） |
| 地图 | 高德 JS API（需 Key，可选；无 Key 降级列表+坐标） |

## 配置

- 后端 `.env`：`DEEPSEEK_API_KEY`、`DEEPSEEK_MODEL=deepseek-v4-flash`、`AMAP_JS_KEY`
- 前端 `.env`：`VITE_API_BASE`（默认指向远程后端）、`VITE_AMAP_JS_KEY`（可选）
- 项目提供 `.env.example` 模板；`.env` 已加入 `.gitignore`，不进入版本库。

## 数据策略

- **当前**：`backend/app/data/beijing_pois.json` 是 6 条手写种子数据，用于验证推荐引擎。这些不是真实 POI，命名和评论为示意。
- **MVP 数据路线**：接入高德地图 POI 搜索/详情官方 API（需要 `AMAP_WEB_KEY`），获取北京真实 POI 的名称、坐标、类型、行政区；再人工为每个 POI 补充 3–5 条“氛围”短句，替换当前种子数据。
- **长期**：如合规可行，再接入大众点评/小红书等 UGC 评论；MVP 阶段不爬取，规避法律与反爬风险。

## 部署

- 远程 Linux 服务器，root 可免密 SSH 登录；需与现有两个 Web 服务隔离。
- 默认端口：后端 `8088`，前端 `8089`。
- 部署前检查端口占用，必要时调整。
- 部署方式优先 Docker Compose；若无 Docker 则使用 systemd。

## 当前状态

- PRD 已整理：`docs/PRD.md`
- 后端已优化：用规则意图解析器替代 LLM 意图解析，推荐接口从 2 次 DeepSeek 调用降为 1 次，实测约 8 秒。
- 前端已优化：按钮加载动画、步骤提示、30 秒超时与错误提示。
- 下一步：接入真实 POI 数据（高德地图 API）。
- 用户补充了开发偏好文档 `PREFERENCE.md`，后续开发需遵循。

## 待确认/待提供

- 高德 JS API Key（可选，用于地图）
- 防火墙端口 8088/8089 是否已开启
- 希望页面访问路径/域名（如 `http://47.89.243.26:8089`）

## 更新记录

- 2026-07-18：创建本文件，搭建后端骨架，完成第一次 git commit 与 push。
- 2026-07-18：实现后端推荐接口 `/recommend` 与反馈接口 `/feedback`，并本地测试通过。
- 2026-07-18：后端部署到远程服务器 8088 端口，健康接口在线可访问。
- 2026-07-18：前端 React 页面部署到 8089 端口；按 `PREFERENCE.md` 修复代理与时区。
- 2026-07-18：优化推荐接口性能（规则意图解析 + 单次 LLM 调用）与前端加载体验。
